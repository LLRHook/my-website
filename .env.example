# GitHub personal access token (optional but recommended)
# Without this, only public repos will be shown via the /users/:username/repos endpoint.
# With a token, the /user/repos endpoint is used, which can also list private repos.
# Create one at: https://github.com/settings/tokens (no special scopes needed for public repos)
GITHUB_TOKEN=
